apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: prometheus
  name: python-test
  namespace: monitoring
spec:
  endpoints:
  - port: "9086"
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      name: python-test